[project]
name = "paper-review-openenv"
version = "0.1.0"
description = "OpenEnv paper review environment"
authors = [
    { name = "Rambabu" }
]
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic"
]

[tool.openenv]
entrypoint = "server.app:app"